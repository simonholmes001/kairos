{
  "name": "@kairos/agents",
  "version": "0.1.0",
  "private": true,
  "description": "KAIROS agent workflow scaffold",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
