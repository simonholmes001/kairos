[project]
name = "kairos-maf"
version = "0.1.0"
description = "KAIROS Microsoft Agent Framework research workflow adapter"
requires-python = ">=3.10"
dependencies = [
  "agent-framework-core>=1.12,<2",
  "agent-framework-orchestrations>=1.12,<2"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
